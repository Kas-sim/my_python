import pandas as pd

df = pd.read_json('TestEmployee.json')

num_of_employees = df['name'].count()
top_five_employees = df.head()

average_salary = df['salary'].mean()
highest_salary = df['salary'].max()
lowest_salary = df['salary'].min()
sum_salary = df['salary'].sum()


grouped = df.groupby('role')['salary'].mean()
roundedSalaries = round(grouped['salary'])

def salaryComparison():
    print("Enter the role you want to compare be compared & compare with \n")
    print(type(grouped))
    role1 = input("Role 1: ")
    role2 = input("Role 2: ")

    if grouped[role1] > grouped[role2]:
        print(f" {role1} earns more than {role2}!")
    elif grouped[role1] < grouped[role2]:
        print(f" {role2} earns more than {role1}!")
    else:
        print(f" {role1} and {role2} both earns equal!")

# grouped = df.groupby('role')['salary'].agg( [ "count", "mean" ])

while True:
    x = int(input("\n0. 0 to Exit \n1. Number of Employees \n2. Check Top 5 Senior Employees \n3. Check Salaries \n4. Compare Salaries b/w two roles \nEnter Operation: "))
    if x == 0:
        break
    elif x == 1:
        print(num_of_employees)
    elif x == 2:
        print(top_five_employees)
    elif x == 3:
            y = int(input("\n1. Average Salary \n2. Highest Salary \n3. Lowest Salary \n4. All info \nEnter Operation: "))
            if y == 1:
                print(f"\n Average Salary -> { round(average_salary) } \n")
            elif y == 2:
                print(f"\n HIghest Salary -> { round(highest_salary) } \n")
            elif y == 3:
                print(f"\n Lowest Salary -> { round(lowest_salary) } \n")
            else:
                print(f"\n Yearly company gives -> { round(sum_salary) } to Employees \n")
                print(roundedSalaries)
                # Salary not being rounded in above
    elif x == 4:
        salaryComparison()   
    else:
        print("Invalid Operation!\n")